#colors=["red","blue","red"]
#cars=["tata","nano","bmw","mercedes"]
#colors[0:2]=["green","yellow"]
#colors.insert(2,"indigo")
#colors.append("bmw")
#colors.extend(cars)
#colors.pop(0)
#colors.remove("bmw")
#colors.clear()
#print(colors)
#newlist=[x for x in cars if "a" in x]
#print(newlist)
#list are changable and ordered by default and it contains different data types to one variable it allows duplicate values
#sort() it arrange elements of the list for descending add reverse true in the parathesis
#copy() to create the copy of the list
#List are used store multiple non-homogeneous data in a single variable
# ordered
# Changable
# Allows duplicate value
# Insert() - insert item  at specified index
# Append() - insert item at the end of the list
# Extend() - it appends elements from one list to another list, it can be used to append different collection
# Remove() - removes item from a specified index
# pop()
# clear() - clears all elements
# range(), len()
# sort() - it will sort the list in ascending order
# copy()